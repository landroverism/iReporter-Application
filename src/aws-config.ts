// AWS Configuration for Amazon Polly

export const awsConfig = {
  region: 'us-east-1',
  credentials: {
    // For demo purposes only - in production, use proper authentication
    // such as Cognito Identity Pool or environment variables
    accessKeyId: 'DEMO_ACCESS_KEY_ID',
    secretAccessKey: 'DEMO_SECRET_ACCESS_KEY'
  }
};
